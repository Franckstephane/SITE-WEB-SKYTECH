# TODO - SkyTech

## Objectif
Rendre la page `service.html` conforme : affichage + **sélection via checkbox** des options, puis enregistrement dans `localStorage` avec total estimé.

## Étapes
- [ ] 1. Modifier `service.html` pour afficher les prestations sous forme de checkbox + zones “Total estimé” et “Options sélectionnées”.
- [ ] 2. Modifier `script.js` : générer les checkbox avec prix, et adapter `ajouterPanier()` pour enregistrer options + total.
- [ ] 3. Tester manuellement : choisir un service, cocher plusieurs options, cliquer “Ajouter au panier”, vérifier `localStorage.panier`.

